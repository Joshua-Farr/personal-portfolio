import styled from "styled-components"

export const Footer = () => {

    const FooterWrapper = styled.div``
    return (
        <FooterWrapper>
            Footer!
        </FooterWrapper>
    )
    
}